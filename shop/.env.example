# ── Chikwafu shop (customer storefront) ────────────────────────────────
# cp .env.example .env
#
# Base URL of the Chikwafu API, INCLUDING the /api prefix — the app calls
# relative paths like /products on top of this.
VITE_API_URL=http://localhost:5000/api
