# ── Chikwafu admin dashboard ───────────────────────────────────────────
# cp .env.example .env

# Base URL of the Chikwafu API. Must include the scheme, no trailing slash.
# The API's ADMIN_URL must in turn allow this app's origin (default :5174).
VITE_API_URL=http://localhost:5000
