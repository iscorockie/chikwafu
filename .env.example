# Point the admin dashboard at the Chikwafu Express API (repo root /server).
# Leave unset to run the storefront and admin against local demo data —
# which is what the static GitHub Pages build does.
VITE_API_URL=http://localhost:5000
